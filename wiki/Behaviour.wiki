#summary Describes the behaviour of the router

<wiki:toc/>

= Introduction =

Add your content here.

= The "tls" Transport Parameter =

SipURIs with a transport parameter set to `tls` will be treated as if they are `sips` URIs with a transport parameter set to `tcp`.